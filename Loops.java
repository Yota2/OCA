public class Loops {
    int i = 0;
        for(long y = 5, i = 4; x < 7 && y < 40; x++, y++) {
        // DOES NOT COMPILE coz redeclaring a Variable in the Initialization Block is not allowed.
        System.out.print(x + " ");
    }
}
